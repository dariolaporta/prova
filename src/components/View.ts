import styled from "styled-components";

export const View = styled.div`
    left: 0;
    right: 0;
    position: absolute;
    top: 20%;
    display: flex;
    justify-content: center;
    align-items: center;
`;
